import { describe, expect, it } from 'vitest'; import { compareSnapshots } from '@/lib/compare/engine';
describe('compareSnapshots',()=>{const base={name:'b',reportDate:'2026-01-01',items:[{issueKey:'A-1',title:'one',team:'IPG',status:'Current Sprint',storyPoints:3,priority:'Medium'},{issueKey:'A-2',title:'two',team:'POS',status:'Ready for Development',storyPoints:5}]}; const cur={name:'c',reportDate:'2026-01-15',items:[{issueKey:'A-1',title:'one',team:'IPG',status:'Remain',storyPoints:8,priority:'High'},{issueKey:'A-3',title:'three',team:'POS',status:'Current Sprint',storyPoints:2}]}; it('detects core changes and metrics',()=>{const r=compareSnapshots(base,cur); expect(r.changes.some(c=>c.changeType==='MOVED_TO_REMAIN')).toBe(true); expect(r.changes.some(c=>c.changeType==='CARRY_OVER')).toBe(true); expect(r.changes.some(c=>c.changeType==='ADDED')).toBe(true); expect(r.changes.some(c=>c.changeType==='REMOVED')).toBe(true); expect(r.summary.storyPointDelta).toBe(2);}); it('uses normalized title and team fallback',()=>{const r=compareSnapshots({name:'b',reportDate:'',items:[{title:' Same title ',team:'IPG',status:'Approved'}]},{name:'c',reportDate:'',items:[{title:'same   title',team:'ipg',status:'Current Sprint'}]}); expect(r.changes.some(c=>c.changeType==='MOVED_TO_CURRENT_SPRINT')).toBe(true); expect(r.changes.some(c=>c.changeType==='ADDED')).toBe(false);});});

'use client'; import { useState } from 'react'; import { Button } from '@/components/ui/button';
const defaultMapping={issueKey:'Issue Key',title:'Title',team:'Team',stakeholderDeputy:'Stakeholder Deputy',priority:'Priority',storyPoints:'Story Points',status:'Status',isBlocked:'Is Blocked',blockerReason:'Blocker Reason'};
export default function Upload(){const [msg,setMsg]=useState(''); async function onSubmit(e:React.FormEvent<HTMLFormElement>){e.preventDefault(); const fd=new FormData(e.currentTarget); fd.set('mapping',JSON.stringify(defaultMapping)); const r=await fetch('/api/upload',{method:'POST',body:fd}); const data=await r.json(); if(!r.ok){setMsg('خطا در اعتبارسنجی ستون‌ها');return} const name=String(fd.get('name')||'Uploaded Snapshot'); await fetch('/api/snapshots',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({name,sprintName:fd.get('sprintName'),reportDate:fd.get('reportDate'),sourceFileName:(fd.get('file') as File).name,items:data.items})}); setMsg(`اسنپ‌شات با ${data.items.length} آیتم ذخیره شد.`)} return <form onSubmit={onSubmit} className="card grid gap-4"><h2 className="text-xl font-bold">بارگذاری Sprint Report</h2><input name="name" className="rounded-xl border p-2" placeholder="نام اسنپ‌شات" required/><input name="sprintName" className="rounded-xl border p-2" placeholder="نام اسپرینت"/><input name="reportDate" type="date" className="rounded-xl border p-2" required/><input name="file" type="file" accept=".xlsx,.xls,.csv" required/><p className="muted">MVP از پروفایل پیش‌فرض ستون‌ها استفاده می‌کند و ساختار ذخیره mapping برای توسعه بعدی آماده است.</p><Button>بارگذاری و ذخیره</Button>{msg&&<p>{msg}</p>}</form>}
